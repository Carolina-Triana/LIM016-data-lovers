 /*let filter = document.getElementById("gender");
    filter.addEventListener('click', () => {
        let alpha = person.map(obj=>obj.gender)
        
        console.log(alpha)})*/

       let filter = document.getElementById("gender");
filter.addEventListener('click', () => {
    let alpha = person.filter((gen) => {
        if (gen.gender === "Female"){
            return true;
        } else {
            return false;
        }
    })
})